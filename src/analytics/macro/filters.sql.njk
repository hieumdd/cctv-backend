{%- macro globalFilter(companyName, div, reg, st, symbol) -%}
    (
        1 = 1
        {% if companyName %}
        and company_name = {{ companyName | quote }}
        {% endif %}
        {% if div %}
        and div = {{ div | quote }}
        {% endif %}
        {% if reg %}
        and reg = {{ reg | quote }}
        {% endif %}
        {% if st %}
        and st = {{ st | quote }}
        {% endif %}
        {% if symbol %}
        and symbol = {{ symbol | quote }}
        {% endif %}
    )
{%- endmacro -%}
